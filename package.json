{
  "name": "zuzel",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Przeglądarkowa gra żużlowa \"kreski\"",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
